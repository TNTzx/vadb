// *** DO NOT EDIT. GENERATED BY PRISMIX. ***
datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Artist {
  id           Int          @id @default(autoincrement())
  name         String
  safeName     String?
  aliases      String?
  description  String
  notes        String?
  genre        String?
  tracks       Int          @default(0)
  status       Status       @default(NONE)
  availability Availability @default(NONE)
  addedAt      DateTime     @default(now())
  updatedAt    DateTime     @updatedAt
  rights       Right[]      @relation(name: "ArtistToRight")
  socials      Social[]     @relation(name: "ArtistToSocial")
}

model Right {
  id       Int     @id @default(autoincrement())
  name     String
  value    Boolean
  artist   Artist  @relation(name: "ArtistToRight", fields: [artistId], references: [id])
  artistId Int
}

model Social {
  id       Int     @id @default(autoincrement())
  link     String
  type     String
  icon     String?
  artist   Artist  @relation(name: "ArtistToSocial", fields: [artistId], references: [id])
  artistId Int
}

model Token {
  id         Int    @id @default(autoincrement())
  token      String
  assignedIp String
}

enum Status {
  NONE
  COMPLETED
  NO_CONTACT
  PENDING
  REQUESTED
}

enum Availability {
  NONE
  VERIFIED
  DISALLOWED
  CONTACT_REQUIRED
  VARIES
}
